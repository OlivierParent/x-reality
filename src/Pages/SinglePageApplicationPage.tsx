import { GroupProps } from "@react-three/fiber";

import SinglePageApplication from "Examples/SinglePageApplication";

/**
 * Page for a Single Page Application.
 *
 * @param {GroupProps} props
 * @returns {JSX.Element}
 */
const SinglePageApplicationPage = (props: GroupProps): JSX.Element => {
  return <SinglePageApplication />;
};

export default SinglePageApplicationPage;
